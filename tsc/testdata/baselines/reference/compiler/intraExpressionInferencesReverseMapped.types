//// [tests/cases/compiler/intraExpressionInferencesReverseMapped.ts] ////

=== intraExpressionInferencesReverseMapped.ts ===
// Intra-expression inferences must flow through reverse mapped (homomorphic mapped type) inferences,
// where the inference for T is made from the entire source object rather than property by property.

declare function f<T>(arg: {
>f : <T>(arg: { [K in keyof T]: { produce: (n: string) => T[K]; consume: (x: T[K]) => void; }; }) => T
>arg : { [K in keyof T]: { produce: (n: string) => T[K]; consume: (x: T[K]) => void; }; }

    [K in keyof T]: {
        produce: (n: string) => T[K];
>produce : (n: string) => T[K]
>n : string

        consume: (x: T[K]) => void;
>consume : (x: T[K]) => void
>x : T[K]

    };
}): T;

// Non-context sensitive producers (worked before)
const r1 = f({ a: { produce: () => "hello", consume: x => x.toLowerCase() } });
>r1 : { a: string; }
>f({ a: { produce: () => "hello", consume: x => x.toLowerCase() } }) : { a: string; }
>f : <T>(arg: { [K in keyof T]: { produce: (n: string) => T[K]; consume: (x: T[K]) => void; }; }) => T
>{ a: { produce: () => "hello", consume: x => x.toLowerCase() } } : { a: { produce: () => string; consume: (x: string) => string; }; }
>a : { produce: () => string; consume: (x: string) => string; }
>{ produce: () => "hello", consume: x => x.toLowerCase() } : { produce: () => string; consume: (x: string) => string; }
>produce : () => string
>() => "hello" : () => string
>"hello" : "hello"
>consume : (x: string) => string
>x => x.toLowerCase() : (x: string) => string
>x : string
>x.toLowerCase() : string
>x.toLowerCase : () => string
>x : string
>toLowerCase : () => string

const r2 = f({ a: { produce: (n: string) => n, consume: x => x.toLowerCase() } });
>r2 : { a: string; }
>f({ a: { produce: (n: string) => n, consume: x => x.toLowerCase() } }) : { a: string; }
>f : <T>(arg: { [K in keyof T]: { produce: (n: string) => T[K]; consume: (x: T[K]) => void; }; }) => T
>{ a: { produce: (n: string) => n, consume: x => x.toLowerCase() } } : { a: { produce: (n: string) => string; consume: (x: string) => string; }; }
>a : { produce: (n: string) => string; consume: (x: string) => string; }
>{ produce: (n: string) => n, consume: x => x.toLowerCase() } : { produce: (n: string) => string; consume: (x: string) => string; }
>produce : (n: string) => string
>(n: string) => n : (n: string) => string
>n : string
>n : string
>consume : (x: string) => string
>x => x.toLowerCase() : (x: string) => string
>x : string
>x.toLowerCase() : string
>x.toLowerCase : () => string
>x : string
>toLowerCase : () => string

// Context sensitive producers
const r3 = f({ a: { produce: n => n, consume: x => x.toLowerCase() } });
>r3 : { a: string; }
>f({ a: { produce: n => n, consume: x => x.toLowerCase() } }) : { a: string; }
>f : <T>(arg: { [K in keyof T]: { produce: (n: string) => T[K]; consume: (x: T[K]) => void; }; }) => T
>{ a: { produce: n => n, consume: x => x.toLowerCase() } } : { a: { produce: (n: string) => string; consume: (x: string) => string; }; }
>a : { produce: (n: string) => string; consume: (x: string) => string; }
>{ produce: n => n, consume: x => x.toLowerCase() } : { produce: (n: string) => string; consume: (x: string) => string; }
>produce : (n: string) => string
>n => n : (n: string) => string
>n : string
>n : string
>consume : (x: string) => string
>x => x.toLowerCase() : (x: string) => string
>x : string
>x.toLowerCase() : string
>x.toLowerCase : () => string
>x : string
>toLowerCase : () => string

const r4 = f({ a: { produce: function () { return "hello"; }, consume: x => x.toLowerCase() } });
>r4 : { a: string; }
>f({ a: { produce: function () { return "hello"; }, consume: x => x.toLowerCase() } }) : { a: string; }
>f : <T>(arg: { [K in keyof T]: { produce: (n: string) => T[K]; consume: (x: T[K]) => void; }; }) => T
>{ a: { produce: function () { return "hello"; }, consume: x => x.toLowerCase() } } : { a: { produce: () => string; consume: (x: string) => string; }; }
>a : { produce: () => string; consume: (x: string) => string; }
>{ produce: function () { return "hello"; }, consume: x => x.toLowerCase() } : { produce: () => string; consume: (x: string) => string; }
>produce : () => string
>function () { return "hello"; } : () => string
>"hello" : "hello"
>consume : (x: string) => string
>x => x.toLowerCase() : (x: string) => string
>x : string
>x.toLowerCase() : string
>x.toLowerCase : () => string
>x : string
>toLowerCase : () => string

const r5 = f({ a: { produce() { return "hello"; }, consume: x => x.toLowerCase() } });
>r5 : { a: string; }
>f({ a: { produce() { return "hello"; }, consume: x => x.toLowerCase() } }) : { a: string; }
>f : <T>(arg: { [K in keyof T]: { produce: (n: string) => T[K]; consume: (x: T[K]) => void; }; }) => T
>{ a: { produce() { return "hello"; }, consume: x => x.toLowerCase() } } : { a: { produce(): string; consume: (x: string) => string; }; }
>a : { produce(): string; consume: (x: string) => string; }
>{ produce() { return "hello"; }, consume: x => x.toLowerCase() } : { produce(): string; consume: (x: string) => string; }
>produce : () => string
>"hello" : "hello"
>consume : (x: string) => string
>x => x.toLowerCase() : (x: string) => string
>x : string
>x.toLowerCase() : string
>x.toLowerCase : () => string
>x : string
>toLowerCase : () => string

// Sibling with an inferable type alongside the context sensitive ones
declare function g<T>(arg: {
>g : <T>(arg: { [K in keyof T]: { produce: (n: string) => T[K]; consume: (x: T[K]) => void; tag?: number; }; }) => T
>arg : { [K in keyof T]: { produce: (n: string) => T[K]; consume: (x: T[K]) => void; tag?: number; }; }

    [K in keyof T]: {
        produce: (n: string) => T[K];
>produce : (n: string) => T[K]
>n : string

        consume: (x: T[K]) => void;
>consume : (x: T[K]) => void
>x : T[K]

        tag?: number;
>tag : number | undefined

    };
}): T;

const r6 = g({ a: { produce: n => n, consume: x => x.toLowerCase(), tag: 1 } });
>r6 : { a: string; }
>g({ a: { produce: n => n, consume: x => x.toLowerCase(), tag: 1 } }) : { a: string; }
>g : <T>(arg: { [K in keyof T]: { produce: (n: string) => T[K]; consume: (x: T[K]) => void; tag?: number; }; }) => T
>{ a: { produce: n => n, consume: x => x.toLowerCase(), tag: 1 } } : { a: { produce: (n: string) => string; consume: (x: string) => string; tag: number; }; }
>a : { produce: (n: string) => string; consume: (x: string) => string; tag: number; }
>{ produce: n => n, consume: x => x.toLowerCase(), tag: 1 } : { produce: (n: string) => string; consume: (x: string) => string; tag: number; }
>produce : (n: string) => string
>n => n : (n: string) => string
>n : string
>n : string
>consume : (x: string) => string
>x => x.toLowerCase() : (x: string) => string
>x : string
>x.toLowerCase() : string
>x.toLowerCase : () => string
>x : string
>toLowerCase : () => string
>tag : number
>1 : 1

// Multiple keys. T is fixed when the first consumer is checked, but members of the reverse mapped
// type are resolved lazily, so producers of later keys still contribute when those keys are read.
const r7 = g({
>r7 : { a: string; b: number; }
>g({    a: { produce: n => n, consume: x => x.toLowerCase() },    b: { produce: () => 1, consume: x => x.toFixed() },}) : { a: string; b: number; }
>g : <T>(arg: { [K in keyof T]: { produce: (n: string) => T[K]; consume: (x: T[K]) => void; tag?: number; }; }) => T
>{    a: { produce: n => n, consume: x => x.toLowerCase() },    b: { produce: () => 1, consume: x => x.toFixed() },} : { a: { produce: (n: string) => string; consume: (x: string) => string; }; b: { produce: () => number; consume: (x: number) => string; }; }

    a: { produce: n => n, consume: x => x.toLowerCase() },
>a : { produce: (n: string) => string; consume: (x: string) => string; }
>{ produce: n => n, consume: x => x.toLowerCase() } : { produce: (n: string) => string; consume: (x: string) => string; }
>produce : (n: string) => string
>n => n : (n: string) => string
>n : string
>n : string
>consume : (x: string) => string
>x => x.toLowerCase() : (x: string) => string
>x : string
>x.toLowerCase() : string
>x.toLowerCase : () => string
>x : string
>toLowerCase : () => string

    b: { produce: () => 1, consume: x => x.toFixed() },
>b : { produce: () => number; consume: (x: number) => string; }
>{ produce: () => 1, consume: x => x.toFixed() } : { produce: () => number; consume: (x: number) => string; }
>produce : () => number
>() => 1 : () => number
>1 : 1
>consume : (x: number) => string
>x => x.toFixed() : (x: number) => string
>x : number
>x.toFixed() : string
>x.toFixed : (fractionDigits?: number) => string
>x : number
>toFixed : (fractionDigits?: number) => string

});
const r8 = g({
>r8 : { b: number; a: string; }
>g({    b: { produce: () => 1, consume: x => x.toFixed() },    a: { produce: n => n, consume: x => x.toLowerCase() },}) : { b: number; a: string; }
>g : <T>(arg: { [K in keyof T]: { produce: (n: string) => T[K]; consume: (x: T[K]) => void; tag?: number; }; }) => T
>{    b: { produce: () => 1, consume: x => x.toFixed() },    a: { produce: n => n, consume: x => x.toLowerCase() },} : { b: { produce: () => number; consume: (x: number) => string; }; a: { produce: (n: string) => string; consume: (x: string) => string; }; }

    b: { produce: () => 1, consume: x => x.toFixed() },
>b : { produce: () => number; consume: (x: number) => string; }
>{ produce: () => 1, consume: x => x.toFixed() } : { produce: () => number; consume: (x: number) => string; }
>produce : () => number
>() => 1 : () => number
>1 : 1
>consume : (x: number) => string
>x => x.toFixed() : (x: number) => string
>x : number
>x.toFixed() : string
>x.toFixed : (fractionDigits?: number) => string
>x : number
>toFixed : (fractionDigits?: number) => string

    a: { produce: n => n, consume: x => x.toLowerCase() },
>a : { produce: (n: string) => string; consume: (x: string) => string; }
>{ produce: n => n, consume: x => x.toLowerCase() } : { produce: (n: string) => string; consume: (x: string) => string; }
>produce : (n: string) => string
>n => n : (n: string) => string
>n : string
>n : string
>consume : (x: string) => string
>x => x.toLowerCase() : (x: string) => string
>x : string
>x.toLowerCase() : string
>x.toLowerCase : () => string
>x : string
>toLowerCase : () => string

});

// Tuples in the template
declare function h<T>(arg: {
>h : <T>(arg: { [K in keyof T]: [(n: string) => T[K], (x: T[K]) => void]; }) => T
>arg : { [K in keyof T]: [(n: string) => T[K], (x: T[K]) => void]; }

    [K in keyof T]: [(n: string) => T[K], (x: T[K]) => void];
>n : string
>x : T[K]

}): T;

const r9 = h({ a: [n => n, x => x.toLowerCase()] });
>r9 : { a: string; }
>h({ a: [n => n, x => x.toLowerCase()] }) : { a: string; }
>h : <T>(arg: { [K in keyof T]: [(n: string) => T[K], (x: T[K]) => void]; }) => T
>{ a: [n => n, x => x.toLowerCase()] } : { a: [(n: string) => string, (x: string) => string]; }
>a : [(n: string) => string, (x: string) => string]
>[n => n, x => x.toLowerCase()] : [(n: string) => string, (x: string) => string]
>n => n : (n: string) => string
>n : string
>n : string
>x => x.toLowerCase() : (x: string) => string
>x : string
>x.toLowerCase() : string
>x.toLowerCase : () => string
>x : string
>toLowerCase : () => string

// Nested reverse mappings
declare function nested<T>(arg: {
>nested : <T>(arg: { [K in keyof T]: { [J in keyof T[K]]: { produce: (n: string) => T[K][J]; consume: (x: T[K][J]) => void; }; }; }) => T
>arg : { [K in keyof T]: { [J in keyof T[K]]: { produce: (n: string) => T[K][J]; consume: (x: T[K][J]) => void; }; }; }

    [K in keyof T]: {
        [J in keyof T[K]]: {
            produce: (n: string) => T[K][J];
>produce : (n: string) => T[K][J]
>n : string

            consume: (x: T[K][J]) => void;
>consume : (x: T[K][J]) => void
>x : T[K][J]

        };
    };
}): T;

const r14 = nested({ a: { b: { produce: n => n, consume: x => x.toLowerCase() } } });
>r14 : { a: { b: string; }; }
>nested({ a: { b: { produce: n => n, consume: x => x.toLowerCase() } } }) : { a: { b: string; }; }
>nested : <T>(arg: { [K in keyof T]: { [J in keyof T[K]]: { produce: (n: string) => T[K][J]; consume: (x: T[K][J]) => void; }; }; }) => T
>{ a: { b: { produce: n => n, consume: x => x.toLowerCase() } } } : { a: { b: { produce: (n: string) => string; consume: (x: string) => string; }; }; }
>a : { b: { produce: (n: string) => string; consume: (x: string) => string; }; }
>{ b: { produce: n => n, consume: x => x.toLowerCase() } } : { b: { produce: (n: string) => string; consume: (x: string) => string; }; }
>b : { produce: (n: string) => string; consume: (x: string) => string; }
>{ produce: n => n, consume: x => x.toLowerCase() } : { produce: (n: string) => string; consume: (x: string) => string; }
>produce : (n: string) => string
>n => n : (n: string) => string
>n : string
>n : string
>consume : (x: string) => string
>x => x.toLowerCase() : (x: string) => string
>x : string
>x.toLowerCase() : string
>x.toLowerCase : () => string
>x : string
>toLowerCase : () => string

// Reverse mapping nested inside a non-mapped structure
declare function wrapped<T>(arg: {
>wrapped : <T>(arg: { items: { [K in keyof T]: { produce: (n: string) => T[K]; consume: (x: T[K]) => void; }; }; other: (x: T) => void; }) => T
>arg : { items: { [K in keyof T]: { produce: (n: string) => T[K]; consume: (x: T[K]) => void; }; }; other: (x: T) => void; }

    items: {
>items : { [K in keyof T]: { produce: (n: string) => T[K]; consume: (x: T[K]) => void; }; }

        [K in keyof T]: {
            produce: (n: string) => T[K];
>produce : (n: string) => T[K]
>n : string

            consume: (x: T[K]) => void;
>consume : (x: T[K]) => void
>x : T[K]

        };
    };
    other: (x: T) => void;
>other : (x: T) => void
>x : T

}): T;

const r15 = wrapped({
>r15 : { a: string; }
>wrapped({    items: { a: { produce: n => n, consume: x => x.toLowerCase() } },    other: t => t.a.toLowerCase(),}) : { a: string; }
>wrapped : <T>(arg: { items: { [K in keyof T]: { produce: (n: string) => T[K]; consume: (x: T[K]) => void; }; }; other: (x: T) => void; }) => T
>{    items: { a: { produce: n => n, consume: x => x.toLowerCase() } },    other: t => t.a.toLowerCase(),} : { items: { a: { produce: (n: string) => string; consume: (x: string) => string; }; }; other: (t: { a: string; }) => string; }

    items: { a: { produce: n => n, consume: x => x.toLowerCase() } },
>items : { a: { produce: (n: string) => string; consume: (x: string) => string; }; }
>{ a: { produce: n => n, consume: x => x.toLowerCase() } } : { a: { produce: (n: string) => string; consume: (x: string) => string; }; }
>a : { produce: (n: string) => string; consume: (x: string) => string; }
>{ produce: n => n, consume: x => x.toLowerCase() } : { produce: (n: string) => string; consume: (x: string) => string; }
>produce : (n: string) => string
>n => n : (n: string) => string
>n : string
>n : string
>consume : (x: string) => string
>x => x.toLowerCase() : (x: string) => string
>x : string
>x.toLowerCase() : string
>x.toLowerCase : () => string
>x : string
>toLowerCase : () => string

    other: t => t.a.toLowerCase(),
>other : (t: { a: string; }) => string
>t => t.a.toLowerCase() : (t: { a: string; }) => string
>t : { a: string; }
>t.a.toLowerCase() : string
>t.a.toLowerCase : () => string
>t.a : string
>t : { a: string; }
>a : string
>toLowerCase : () => string

});

// Two type parameters fixed at different points; the types recorded for earlier sites must remain
// available when the second type parameter is fixed.
declare function two<T, U>(arg: {
>two : <T, U>(arg: { first: { [K in keyof T]: { produce: (n: string) => T[K]; consume: (x: T[K]) => void; }; }; second: { [K in keyof U]: { produce: (n: string) => U[K]; consume: (x: U[K], t: T) => void; }; }; }) => [T, U]
>arg : { first: { [K in keyof T]: { produce: (n: string) => T[K]; consume: (x: T[K]) => void; }; }; second: { [K in keyof U]: { produce: (n: string) => U[K]; consume: (x: U[K], t: T) => void; }; }; }

    first: { [K in keyof T]: { produce: (n: string) => T[K]; consume: (x: T[K]) => void } };
>first : { [K in keyof T]: { produce: (n: string) => T[K]; consume: (x: T[K]) => void; }; }
>produce : (n: string) => T[K]
>n : string
>consume : (x: T[K]) => void
>x : T[K]

    second: { [K in keyof U]: { produce: (n: string) => U[K]; consume: (x: U[K], t: T) => void } };
>second : { [K in keyof U]: { produce: (n: string) => U[K]; consume: (x: U[K], t: T) => void; }; }
>produce : (n: string) => U[K]
>n : string
>consume : (x: U[K], t: T) => void
>x : U[K]
>t : T

}): [T, U];

const r16 = two({
>r16 : [{ a: string; }, { b: number; }]
>two({    first: { a: { produce: n => n, consume: x => x.toLowerCase() } },    second: { b: { produce: n => n.length, consume: (x, t) => x.toFixed() + t.a.toLowerCase() } },}) : [{ a: string; }, { b: number; }]
>two : <T, U>(arg: { first: { [K in keyof T]: { produce: (n: string) => T[K]; consume: (x: T[K]) => void; }; }; second: { [K in keyof U]: { produce: (n: string) => U[K]; consume: (x: U[K], t: T) => void; }; }; }) => [T, U]
>{    first: { a: { produce: n => n, consume: x => x.toLowerCase() } },    second: { b: { produce: n => n.length, consume: (x, t) => x.toFixed() + t.a.toLowerCase() } },} : { first: { a: { produce: (n: string) => string; consume: (x: string) => string; }; }; second: { b: { produce: (n: string) => number; consume: (x: number, t: { a: string; }) => string; }; }; }

    first: { a: { produce: n => n, consume: x => x.toLowerCase() } },
>first : { a: { produce: (n: string) => string; consume: (x: string) => string; }; }
>{ a: { produce: n => n, consume: x => x.toLowerCase() } } : { a: { produce: (n: string) => string; consume: (x: string) => string; }; }
>a : { produce: (n: string) => string; consume: (x: string) => string; }
>{ produce: n => n, consume: x => x.toLowerCase() } : { produce: (n: string) => string; consume: (x: string) => string; }
>produce : (n: string) => string
>n => n : (n: string) => string
>n : string
>n : string
>consume : (x: string) => string
>x => x.toLowerCase() : (x: string) => string
>x : string
>x.toLowerCase() : string
>x.toLowerCase : () => string
>x : string
>toLowerCase : () => string

    second: { b: { produce: n => n.length, consume: (x, t) => x.toFixed() + t.a.toLowerCase() } },
>second : { b: { produce: (n: string) => number; consume: (x: number, t: { a: string; }) => string; }; }
>{ b: { produce: n => n.length, consume: (x, t) => x.toFixed() + t.a.toLowerCase() } } : { b: { produce: (n: string) => number; consume: (x: number, t: { a: string; }) => string; }; }
>b : { produce: (n: string) => number; consume: (x: number, t: { a: string; }) => string; }
>{ produce: n => n.length, consume: (x, t) => x.toFixed() + t.a.toLowerCase() } : { produce: (n: string) => number; consume: (x: number, t: { a: string; }) => string; }
>produce : (n: string) => number
>n => n.length : (n: string) => number
>n : string
>n.length : number
>n : string
>length : number
>consume : (x: number, t: { a: string; }) => string
>(x, t) => x.toFixed() + t.a.toLowerCase() : (x: number, t: { a: string; }) => string
>x : number
>t : { a: string; }
>x.toFixed() + t.a.toLowerCase() : string
>x.toFixed() : string
>x.toFixed : (fractionDigits?: number) => string
>x : number
>toFixed : (fractionDigits?: number) => string
>t.a.toLowerCase() : string
>t.a.toLowerCase : () => string
>t.a : string
>t : { a: string; }
>a : string
>toLowerCase : () => string

});

// Errors are still reported
const r17 = f({ a: { produce: n => n, consume: x => x.toFixed() } });
>r17 : { a: string; }
>f({ a: { produce: n => n, consume: x => x.toFixed() } }) : { a: string; }
>f : <T>(arg: { [K in keyof T]: { produce: (n: string) => T[K]; consume: (x: T[K]) => void; }; }) => T
>{ a: { produce: n => n, consume: x => x.toFixed() } } : { a: { produce: (n: string) => string; consume: (x: string) => any; }; }
>a : { produce: (n: string) => string; consume: (x: string) => any; }
>{ produce: n => n, consume: x => x.toFixed() } : { produce: (n: string) => string; consume: (x: string) => any; }
>produce : (n: string) => string
>n => n : (n: string) => string
>n : string
>n : string
>consume : (x: string) => any
>x => x.toFixed() : (x: string) => any
>x : string
>x.toFixed() : any
>x.toFixed : any
>x : string
>toFixed : any

// Closures returning the producer/consumer object
declare function thunk<T>(arg: {
>thunk : <T>(arg: { [K in keyof T]: () => { produce: (n: string) => T[K]; consume: (x: T[K]) => void; }; }) => T
>arg : { [K in keyof T]: () => { produce: (n: string) => T[K]; consume: (x: T[K]) => void; }; }

    [K in keyof T]: () => { produce: (n: string) => T[K]; consume: (x: T[K]) => void };
>produce : (n: string) => T[K]
>n : string
>consume : (x: T[K]) => void
>x : T[K]

}): T;

const r18 = thunk({
>r18 : { a: string; b: number; }
>thunk({    a() { return { produce: n => n, consume: x => x.toLowerCase() }; },    b: () => ({ produce: n => n.length, consume: x => x.toFixed() }),}) : { a: string; b: number; }
>thunk : <T>(arg: { [K in keyof T]: () => { produce: (n: string) => T[K]; consume: (x: T[K]) => void; }; }) => T
>{    a() { return { produce: n => n, consume: x => x.toLowerCase() }; },    b: () => ({ produce: n => n.length, consume: x => x.toFixed() }),} : { a(): { produce: (n: string) => string; consume: (x: string) => string; }; b: () => { produce: (n: string) => number; consume: (x: number) => string; }; }

    a() { return { produce: n => n, consume: x => x.toLowerCase() }; },
>a : () => { produce: (n: string) => string; consume: (x: string) => string; }
>{ produce: n => n, consume: x => x.toLowerCase() } : { produce: (n: string) => string; consume: (x: string) => string; }
>produce : (n: string) => string
>n => n : (n: string) => string
>n : string
>n : string
>consume : (x: string) => string
>x => x.toLowerCase() : (x: string) => string
>x : string
>x.toLowerCase() : string
>x.toLowerCase : () => string
>x : string
>toLowerCase : () => string

    b: () => ({ produce: n => n.length, consume: x => x.toFixed() }),
>b : () => { produce: (n: string) => number; consume: (x: number) => string; }
>() => ({ produce: n => n.length, consume: x => x.toFixed() }) : () => { produce: (n: string) => number; consume: (x: number) => string; }
>({ produce: n => n.length, consume: x => x.toFixed() }) : { produce: (n: string) => number; consume: (x: number) => string; }
>{ produce: n => n.length, consume: x => x.toFixed() } : { produce: (n: string) => number; consume: (x: number) => string; }
>produce : (n: string) => number
>n => n.length : (n: string) => number
>n : string
>n.length : number
>n : string
>length : number
>consume : (x: number) => string
>x => x.toFixed() : (x: number) => string
>x : number
>x.toFixed() : string
>x.toFixed : (fractionDigits?: number) => string
>x : number
>toFixed : (fractionDigits?: number) => string

});

declare function callback<T>(arg: {
>callback : <T>(arg: { [K in keyof T]: (flag: boolean) => { produce: (n: string) => T[K]; consume: (x: T[K]) => void; }; }) => T
>arg : { [K in keyof T]: (flag: boolean) => { produce: (n: string) => T[K]; consume: (x: T[K]) => void; }; }

    [K in keyof T]: (flag: boolean) => { produce: (n: string) => T[K]; consume: (x: T[K]) => void };
>flag : boolean
>produce : (n: string) => T[K]
>n : string
>consume : (x: T[K]) => void
>x : T[K]

}): T;

const r19 = callback({
>r19 : { a: string; b: number; }
>callback({    a(flag) { return { produce: n => n, consume: x => x.toLowerCase() }; },    b: flag => ({ produce: n => n.length, consume: x => x.toFixed() }),}) : { a: string; b: number; }
>callback : <T>(arg: { [K in keyof T]: (flag: boolean) => { produce: (n: string) => T[K]; consume: (x: T[K]) => void; }; }) => T
>{    a(flag) { return { produce: n => n, consume: x => x.toLowerCase() }; },    b: flag => ({ produce: n => n.length, consume: x => x.toFixed() }),} : { a(flag: boolean): { produce: (n: string) => string; consume: (x: string) => string; }; b: (flag: boolean) => { produce: (n: string) => number; consume: (x: number) => string; }; }

    a(flag) { return { produce: n => n, consume: x => x.toLowerCase() }; },
>a : (flag: boolean) => { produce: (n: string) => string; consume: (x: string) => string; }
>flag : boolean
>{ produce: n => n, consume: x => x.toLowerCase() } : { produce: (n: string) => string; consume: (x: string) => string; }
>produce : (n: string) => string
>n => n : (n: string) => string
>n : string
>n : string
>consume : (x: string) => string
>x => x.toLowerCase() : (x: string) => string
>x : string
>x.toLowerCase() : string
>x.toLowerCase : () => string
>x : string
>toLowerCase : () => string

    b: flag => ({ produce: n => n.length, consume: x => x.toFixed() }),
>b : (flag: boolean) => { produce: (n: string) => number; consume: (x: number) => string; }
>flag => ({ produce: n => n.length, consume: x => x.toFixed() }) : (flag: boolean) => { produce: (n: string) => number; consume: (x: number) => string; }
>flag : boolean
>({ produce: n => n.length, consume: x => x.toFixed() }) : { produce: (n: string) => number; consume: (x: number) => string; }
>{ produce: n => n.length, consume: x => x.toFixed() } : { produce: (n: string) => number; consume: (x: number) => string; }
>produce : (n: string) => number
>n => n.length : (n: string) => number
>n : string
>n.length : number
>n : string
>length : number
>consume : (x: number) => string
>x => x.toFixed() : (x: number) => string
>x : number
>x.toFixed() : string
>x.toFixed : (fractionDigits?: number) => string
>x : number
>toFixed : (fractionDigits?: number) => string

});

declare function tupleThunk<T>(arg: {
>tupleThunk : <T>(arg: { [K in keyof T]: () => [(n: string) => T[K], (x: T[K]) => void]; }) => T
>arg : { [K in keyof T]: () => [(n: string) => T[K], (x: T[K]) => void]; }

    [K in keyof T]: () => [(n: string) => T[K], (x: T[K]) => void];
>n : string
>x : T[K]

}): T;

const r20 = tupleThunk({
>r20 : { a: string; b: number; }
>tupleThunk({    a() { return [n => n, x => x.toLowerCase()]; },    b() { return [n => n.length, x => x.toFixed()]; },}) : { a: string; b: number; }
>tupleThunk : <T>(arg: { [K in keyof T]: () => [(n: string) => T[K], (x: T[K]) => void]; }) => T
>{    a() { return [n => n, x => x.toLowerCase()]; },    b() { return [n => n.length, x => x.toFixed()]; },} : { a(): [(n: string) => string, (x: string) => string]; b(): [(n: string) => number, (x: number) => string]; }

    a() { return [n => n, x => x.toLowerCase()]; },
>a : () => [(n: string) => string, (x: string) => string]
>[n => n, x => x.toLowerCase()] : [(n: string) => string, (x: string) => string]
>n => n : (n: string) => string
>n : string
>n : string
>x => x.toLowerCase() : (x: string) => string
>x : string
>x.toLowerCase() : string
>x.toLowerCase : () => string
>x : string
>toLowerCase : () => string

    b() { return [n => n.length, x => x.toFixed()]; },
>b : () => [(n: string) => number, (x: number) => string]
>[n => n.length, x => x.toFixed()] : [(n: string) => number, (x: number) => string]
>n => n.length : (n: string) => number
>n : string
>n.length : number
>n : string
>length : number
>x => x.toFixed() : (x: number) => string
>x : number
>x.toFixed() : string
>x.toFixed : (fractionDigits?: number) => string
>x : number
>toFixed : (fractionDigits?: number) => string

});

// Intersections of mapped types over different type parameters
declare function both<T1, T2>(arg: {
>both : <T1, T2>(arg: { [K in keyof T1]: { produce1: (n: string) => T1[K]; consume1: (x: T1[K]) => void; }; } & { [K in keyof T2]: { produce2: (n: string) => T2[K]; consume2: (x: T2[K]) => void; }; }) => [T1, T2]
>arg : { [K in keyof T1]: { produce1: (n: string) => T1[K]; consume1: (x: T1[K]) => void; }; } & { [K in keyof T2]: { produce2: (n: string) => T2[K]; consume2: (x: T2[K]) => void; }; }

    [K in keyof T1]: { produce1: (n: string) => T1[K]; consume1: (x: T1[K]) => void };
>produce1 : (n: string) => T1[K]
>n : string
>consume1 : (x: T1[K]) => void
>x : T1[K]

} & {
    [K in keyof T2]: { produce2: (n: string) => T2[K]; consume2: (x: T2[K]) => void };
>produce2 : (n: string) => T2[K]
>n : string
>consume2 : (x: T2[K]) => void
>x : T2[K]

}): [T1, T2];

const r21 = both({
>r21 : [{ a: string; b: number; }, { a: string[]; b: { v: string; }; }]
>both({    a: {        produce1: n => n, consume1: x => x.toLowerCase(),        produce2: n => [n], consume2: x => x[0].toLowerCase(),    },    b: {        produce1: n => n.length, consume1: x => x.toFixed(),        produce2: n => ({ v: n }), consume2: x => x.v.toLowerCase(),    },}) : [{ a: string; b: number; }, { a: string[]; b: { v: string; }; }]
>both : <T1, T2>(arg: { [K in keyof T1]: { produce1: (n: string) => T1[K]; consume1: (x: T1[K]) => void; }; } & { [K in keyof T2]: { produce2: (n: string) => T2[K]; consume2: (x: T2[K]) => void; }; }) => [T1, T2]
>{    a: {        produce1: n => n, consume1: x => x.toLowerCase(),        produce2: n => [n], consume2: x => x[0].toLowerCase(),    },    b: {        produce1: n => n.length, consume1: x => x.toFixed(),        produce2: n => ({ v: n }), consume2: x => x.v.toLowerCase(),    },} : { a: { produce1: (n: string) => string; consume1: (x: string) => string; produce2: (n: string) => string[]; consume2: (x: string[]) => string; }; b: { produce1: (n: string) => number; consume1: (x: number) => string; produce2: (n: string) => { v: string; }; consume2: (x: { v: string; }) => string; }; }

    a: {
>a : { produce1: (n: string) => string; consume1: (x: string) => string; produce2: (n: string) => string[]; consume2: (x: string[]) => string; }
>{        produce1: n => n, consume1: x => x.toLowerCase(),        produce2: n => [n], consume2: x => x[0].toLowerCase(),    } : { produce1: (n: string) => string; consume1: (x: string) => string; produce2: (n: string) => string[]; consume2: (x: string[]) => string; }

        produce1: n => n, consume1: x => x.toLowerCase(),
>produce1 : (n: string) => string
>n => n : (n: string) => string
>n : string
>n : string
>consume1 : (x: string) => string
>x => x.toLowerCase() : (x: string) => string
>x : string
>x.toLowerCase() : string
>x.toLowerCase : () => string
>x : string
>toLowerCase : () => string

        produce2: n => [n], consume2: x => x[0].toLowerCase(),
>produce2 : (n: string) => string[]
>n => [n] : (n: string) => string[]
>n : string
>[n] : string[]
>n : string
>consume2 : (x: string[]) => string
>x => x[0].toLowerCase() : (x: string[]) => string
>x : string[]
>x[0].toLowerCase() : string
>x[0].toLowerCase : () => string
>x[0] : string
>x : string[]
>0 : 0
>toLowerCase : () => string

    },
    b: {
>b : { produce1: (n: string) => number; consume1: (x: number) => string; produce2: (n: string) => { v: string; }; consume2: (x: { v: string; }) => string; }
>{        produce1: n => n.length, consume1: x => x.toFixed(),        produce2: n => ({ v: n }), consume2: x => x.v.toLowerCase(),    } : { produce1: (n: string) => number; consume1: (x: number) => string; produce2: (n: string) => { v: string; }; consume2: (x: { v: string; }) => string; }

        produce1: n => n.length, consume1: x => x.toFixed(),
>produce1 : (n: string) => number
>n => n.length : (n: string) => number
>n : string
>n.length : number
>n : string
>length : number
>consume1 : (x: number) => string
>x => x.toFixed() : (x: number) => string
>x : number
>x.toFixed() : string
>x.toFixed : (fractionDigits?: number) => string
>x : number
>toFixed : (fractionDigits?: number) => string

        produce2: n => ({ v: n }), consume2: x => x.v.toLowerCase(),
>produce2 : (n: string) => { v: string; }
>n => ({ v: n }) : (n: string) => { v: string; }
>n : string
>({ v: n }) : { v: string; }
>{ v: n } : { v: string; }
>v : string
>n : string
>consume2 : (x: { v: string; }) => string
>x => x.v.toLowerCase() : (x: { v: string; }) => string
>x : { v: string; }
>x.v.toLowerCase() : string
>x.v.toLowerCase : () => string
>x.v : string
>x : { v: string; }
>v : string
>toLowerCase : () => string

    },
});

// A mapped type intersected with a concrete member
declare function mixed<T1, T2>(arg: {
>mixed : <T1, T2>(arg: { [K in keyof T1]: { produce: (n: string) => [T1[K], any]; consume: (x: T1[K]) => void; }; } & { a: { produce: (n: string) => [any, T2]; consume2: (x: T2) => void; }; }) => [T1, T2]
>arg : { [K in keyof T1]: { produce: (n: string) => [T1[K], any]; consume: (x: T1[K]) => void; }; } & { a: { produce: (n: string) => [any, T2]; consume2: (x: T2) => void; }; }

    [K in keyof T1]: { produce: (n: string) => [T1[K], any]; consume: (x: T1[K]) => void };
>produce : (n: string) => [T1[K], any]
>n : string
>consume : (x: T1[K]) => void
>x : T1[K]

} & {
    a: { produce: (n: string) => [any, T2]; consume2: (x: T2) => void };
>a : { produce: (n: string) => [any, T2]; consume2: (x: T2) => void; }
>produce : (n: string) => [any, T2]
>n : string
>consume2 : (x: T2) => void
>x : T2

}): [T1, T2];

const r22 = mixed({
>r22 : [{ a: string; b: number; }, string[]]
>mixed({    a: {        produce: n => [n, [n]],        consume2: x => x[0].toLowerCase(),        consume: x => x.toLowerCase(),    },    b: {        produce: n => [n.length, null],        consume: x => x.toFixed(),    },}) : [{ a: string; b: number; }, string[]]
>mixed : <T1, T2>(arg: { [K in keyof T1]: { produce: (n: string) => [T1[K], any]; consume: (x: T1[K]) => void; }; } & { a: { produce: (n: string) => [any, T2]; consume2: (x: T2) => void; }; }) => [T1, T2]
>{    a: {        produce: n => [n, [n]],        consume2: x => x[0].toLowerCase(),        consume: x => x.toLowerCase(),    },    b: {        produce: n => [n.length, null],        consume: x => x.toFixed(),    },} : { a: { produce: (n: string) => [string, string[]]; consume2: (x: string[]) => string; consume: (x: string) => string; }; b: { produce: (n: string) => [number, null]; consume: (x: number) => string; }; }

    a: {
>a : { produce: (n: string) => [string, string[]]; consume2: (x: string[]) => string; consume: (x: string) => string; }
>{        produce: n => [n, [n]],        consume2: x => x[0].toLowerCase(),        consume: x => x.toLowerCase(),    } : { produce: (n: string) => [string, string[]]; consume2: (x: string[]) => string; consume: (x: string) => string; }

        produce: n => [n, [n]],
>produce : (n: string) => [string, string[]]
>n => [n, [n]] : (n: string) => [string, string[]]
>n : string
>[n, [n]] : [string, string[]]
>n : string
>[n] : string[]
>n : string

        consume2: x => x[0].toLowerCase(),
>consume2 : (x: string[]) => string
>x => x[0].toLowerCase() : (x: string[]) => string
>x : string[]
>x[0].toLowerCase() : string
>x[0].toLowerCase : () => string
>x[0] : string
>x : string[]
>0 : 0
>toLowerCase : () => string

        consume: x => x.toLowerCase(),
>consume : (x: string) => string
>x => x.toLowerCase() : (x: string) => string
>x : string
>x.toLowerCase() : string
>x.toLowerCase : () => string
>x : string
>toLowerCase : () => string

    },
    b: {
>b : { produce: (n: string) => [number, null]; consume: (x: number) => string; }
>{        produce: n => [n.length, null],        consume: x => x.toFixed(),    } : { produce: (n: string) => [number, null]; consume: (x: number) => string; }

        produce: n => [n.length, null],
>produce : (n: string) => [number, null]
>n => [n.length, null] : (n: string) => [number, null]
>n : string
>[n.length, null] : [number, null]
>n.length : number
>n : string
>length : number

        consume: x => x.toFixed(),
>consume : (x: number) => string
>x => x.toFixed() : (x: number) => string
>x : number
>x.toFixed() : string
>x.toFixed : (fractionDigits?: number) => string
>x : number
>toFixed : (fractionDigits?: number) => string

    },
});

// A property that is never read during the call is completed from the checked argument
const r23 = f({
>r23 : { a: string; b: number; }
>f({    a: { produce: n => n, consume: x => x.toLowerCase() },    b: { produce: n => n.length, consume: () => {} },}) : { a: string; b: number; }
>f : <T>(arg: { [K in keyof T]: { produce: (n: string) => T[K]; consume: (x: T[K]) => void; }; }) => T
>{    a: { produce: n => n, consume: x => x.toLowerCase() },    b: { produce: n => n.length, consume: () => {} },} : { a: { produce: (n: string) => string; consume: (x: string) => string; }; b: { produce: (n: string) => number; consume: () => void; }; }

    a: { produce: n => n, consume: x => x.toLowerCase() },
>a : { produce: (n: string) => string; consume: (x: string) => string; }
>{ produce: n => n, consume: x => x.toLowerCase() } : { produce: (n: string) => string; consume: (x: string) => string; }
>produce : (n: string) => string
>n => n : (n: string) => string
>n : string
>n : string
>consume : (x: string) => string
>x => x.toLowerCase() : (x: string) => string
>x : string
>x.toLowerCase() : string
>x.toLowerCase : () => string
>x : string
>toLowerCase : () => string

    b: { produce: n => n.length, consume: () => {} },
>b : { produce: (n: string) => number; consume: () => void; }
>{ produce: n => n.length, consume: () => {} } : { produce: (n: string) => number; consume: () => void; }
>produce : (n: string) => number
>n => n.length : (n: string) => number
>n : string
>n.length : number
>n : string
>length : number
>consume : () => void
>() => {} : () => void

});
r23.b.toFixed();
>r23.b.toFixed() : string
>r23.b.toFixed : (fractionDigits?: number) => string
>r23.b : number
>r23 : { a: string; b: number; }
>b : number
>toFixed : (fractionDigits?: number) => string

// Keys whose producers were checked earlier, read through a later key's consumer of the whole type
declare function whole<T>(arg: {
>whole : <T>(arg: { [K in keyof T]: { seed?: T[K]; produce?: (n: string) => T[K]; consume?: (x: T[K]) => void; consumeAll?: (x: T) => void; }; }) => T
>arg : { [K in keyof T]: { seed?: T[K]; produce?: (n: string) => T[K]; consume?: (x: T[K]) => void; consumeAll?: (x: T) => void; }; }

    [K in keyof T]: {
        seed?: T[K];
>seed : T[K] | undefined

        produce?: (n: string) => T[K];
>produce : ((n: string) => T[K]) | undefined
>n : string

        consume?: (x: T[K]) => void;
>consume : ((x: T[K]) => void) | undefined
>x : T[K]

        consumeAll?: (x: T) => void;
>consumeAll : ((x: T) => void) | undefined
>x : T

    };
}): T;

const r24 = whole({
>r24 : { earlier: { value: string; }; later: string; partial: { value: string; }; full: number; }
>whole({    earlier: { produce: n => ({ value: n }) },    later: { produce: n => n, consumeAll: x => x.earlier.value.toLowerCase() },    partial: { seed: { value: "known" }, consume: x => x.value.toLowerCase() },    full: { seed: 123 },}) : { earlier: { value: string; }; later: string; partial: { value: string; }; full: number; }
>whole : <T>(arg: { [K in keyof T]: { seed?: T[K]; produce?: (n: string) => T[K]; consume?: (x: T[K]) => void; consumeAll?: (x: T) => void; }; }) => T
>{    earlier: { produce: n => ({ value: n }) },    later: { produce: n => n, consumeAll: x => x.earlier.value.toLowerCase() },    partial: { seed: { value: "known" }, consume: x => x.value.toLowerCase() },    full: { seed: 123 },} : { earlier: { produce: (n: string) => { value: string; }; }; later: { produce: (n: string) => string; consumeAll: (x: { earlier: { value: string; }; later: string; partial: { value: string; }; full: number; }) => string; }; partial: { seed: { value: string; }; consume: (x: { value: string; }) => string; }; full: { seed: number; }; }

    earlier: { produce: n => ({ value: n }) },
>earlier : { produce: (n: string) => { value: string; }; }
>{ produce: n => ({ value: n }) } : { produce: (n: string) => { value: string; }; }
>produce : (n: string) => { value: string; }
>n => ({ value: n }) : (n: string) => { value: string; }
>n : string
>({ value: n }) : { value: string; }
>{ value: n } : { value: string; }
>value : string
>n : string

    later: { produce: n => n, consumeAll: x => x.earlier.value.toLowerCase() },
>later : { produce: (n: string) => string; consumeAll: (x: { earlier: { value: string; }; later: string; partial: { value: string; }; full: number; }) => string; }
>{ produce: n => n, consumeAll: x => x.earlier.value.toLowerCase() } : { produce: (n: string) => string; consumeAll: (x: { earlier: { value: string; }; later: string; partial: { value: string; }; full: number; }) => string; }
>produce : (n: string) => string
>n => n : (n: string) => string
>n : string
>n : string
>consumeAll : (x: { earlier: { value: string; }; later: string; partial: { value: string; }; full: number; }) => string
>x => x.earlier.value.toLowerCase() : (x: { earlier: { value: string; }; later: string; partial: { value: string; }; full: number; }) => string
>x : { earlier: { value: string; }; later: string; partial: { value: string; }; full: number; }
>x.earlier.value.toLowerCase() : string
>x.earlier.value.toLowerCase : () => string
>x.earlier.value : string
>x.earlier : { value: string; }
>x : { earlier: { value: string; }; later: string; partial: { value: string; }; full: number; }
>earlier : { value: string; }
>value : string
>toLowerCase : () => string

    partial: { seed: { value: "known" }, consume: x => x.value.toLowerCase() },
>partial : { seed: { value: string; }; consume: (x: { value: string; }) => string; }
>{ seed: { value: "known" }, consume: x => x.value.toLowerCase() } : { seed: { value: string; }; consume: (x: { value: string; }) => string; }
>seed : { value: string; }
>{ value: "known" } : { value: string; }
>value : string
>"known" : "known"
>consume : (x: { value: string; }) => string
>x => x.value.toLowerCase() : (x: { value: string; }) => string
>x : { value: string; }
>x.value.toLowerCase() : string
>x.value.toLowerCase : () => string
>x.value : string
>x : { value: string; }
>value : string
>toLowerCase : () => string

    full: { seed: 123 },
>full : { seed: number; }
>{ seed: 123 } : { seed: number; }
>seed : number
>123 : 123

});
r24.earlier.value.toLowerCase();
>r24.earlier.value.toLowerCase() : string
>r24.earlier.value.toLowerCase : () => string
>r24.earlier.value : string
>r24.earlier : { value: string; }
>r24 : { earlier: { value: string; }; later: string; partial: { value: string; }; full: number; }
>earlier : { value: string; }
>value : string
>toLowerCase : () => string

r24.later.toLowerCase();
>r24.later.toLowerCase() : string
>r24.later.toLowerCase : () => string
>r24.later : string
>r24 : { earlier: { value: string; }; later: string; partial: { value: string; }; full: number; }
>later : string
>toLowerCase : () => string

r24.partial.value.toLowerCase();
>r24.partial.value.toLowerCase() : string
>r24.partial.value.toLowerCase : () => string
>r24.partial.value : string
>r24.partial : { value: string; }
>r24 : { earlier: { value: string; }; later: string; partial: { value: string; }; full: number; }
>partial : { value: string; }
>value : string
>toLowerCase : () => string

r24.full.toFixed();
>r24.full.toFixed() : string
>r24.full.toFixed : (fractionDigits?: number) => string
>r24.full : number
>r24 : { earlier: { value: string; }; later: string; partial: { value: string; }; full: number; }
>full : number
>toFixed : (fractionDigits?: number) => string

declare function indexed<T extends { earlier: unknown }>(arg: {
>indexed : <T extends { earlier: unknown; }>(arg: { [K in keyof T]: { produce?: (n: string) => T[K]; consumeEarlier?: (x: T["earlier"]) => void; }; }) => T
>earlier : unknown
>arg : { [K in keyof T]: { produce?: (n: string) => T[K]; consumeEarlier?: (x: T["earlier"]) => void; }; }

    [K in keyof T]: {
        produce?: (n: string) => T[K];
>produce : ((n: string) => T[K]) | undefined
>n : string

        consumeEarlier?: (x: T["earlier"]) => void;
>consumeEarlier : ((x: T["earlier"]) => void) | undefined
>x : T["earlier"]

    };
}): T;

const r25 = indexed({
>r25 : { earlier: { value: string; }; later: string; }
>indexed({    earlier: { produce: n => ({ value: n }) },    later: { produce: n => n, consumeEarlier: x => x.value.toLowerCase() },}) : { earlier: { value: string; }; later: string; }
>indexed : <T extends { earlier: unknown; }>(arg: { [K in keyof T]: { produce?: (n: string) => T[K]; consumeEarlier?: (x: T["earlier"]) => void; }; }) => T
>{    earlier: { produce: n => ({ value: n }) },    later: { produce: n => n, consumeEarlier: x => x.value.toLowerCase() },} : { earlier: { produce: (n: string) => { value: string; }; }; later: { produce: (n: string) => string; consumeEarlier: (x: { value: string; }) => string; }; }

    earlier: { produce: n => ({ value: n }) },
>earlier : { produce: (n: string) => { value: string; }; }
>{ produce: n => ({ value: n }) } : { produce: (n: string) => { value: string; }; }
>produce : (n: string) => { value: string; }
>n => ({ value: n }) : (n: string) => { value: string; }
>n : string
>({ value: n }) : { value: string; }
>{ value: n } : { value: string; }
>value : string
>n : string

    later: { produce: n => n, consumeEarlier: x => x.value.toLowerCase() },
>later : { produce: (n: string) => string; consumeEarlier: (x: { value: string; }) => string; }
>{ produce: n => n, consumeEarlier: x => x.value.toLowerCase() } : { produce: (n: string) => string; consumeEarlier: (x: { value: string; }) => string; }
>produce : (n: string) => string
>n => n : (n: string) => string
>n : string
>n : string
>consumeEarlier : (x: { value: string; }) => string
>x => x.value.toLowerCase() : (x: { value: string; }) => string
>x : { value: string; }
>x.value.toLowerCase() : string
>x.value.toLowerCase : () => string
>x.value : string
>x : { value: string; }
>value : string
>toLowerCase : () => string

});
r25.earlier.value.toLowerCase();
>r25.earlier.value.toLowerCase() : string
>r25.earlier.value.toLowerCase : () => string
>r25.earlier.value : string
>r25.earlier : { value: string; }
>r25 : { earlier: { value: string; }; later: string; }
>earlier : { value: string; }
>value : string
>toLowerCase : () => string

r25.later.toLowerCase();
>r25.later.toLowerCase() : string
>r25.later.toLowerCase : () => string
>r25.later : string
>r25 : { earlier: { value: string; }; later: string; }
>later : string
>toLowerCase : () => string

// A key with no producer at all stays unknown
const r26 = whole({
>r26 : { earlier: { value: string; }; later: unknown; }
>whole({    earlier: { produce: n => ({ value: n }) },    later: { consumeAll: x => x.earlier.value.toLowerCase() },}) : { earlier: { value: string; }; later: unknown; }
>whole : <T>(arg: { [K in keyof T]: { seed?: T[K]; produce?: (n: string) => T[K]; consume?: (x: T[K]) => void; consumeAll?: (x: T) => void; }; }) => T
>{    earlier: { produce: n => ({ value: n }) },    later: { consumeAll: x => x.earlier.value.toLowerCase() },} : { earlier: { produce: (n: string) => { value: string; }; }; later: { consumeAll: (x: { earlier: { value: string; }; later: unknown; }) => string; }; }

    earlier: { produce: n => ({ value: n }) },
>earlier : { produce: (n: string) => { value: string; }; }
>{ produce: n => ({ value: n }) } : { produce: (n: string) => { value: string; }; }
>produce : (n: string) => { value: string; }
>n => ({ value: n }) : (n: string) => { value: string; }
>n : string
>({ value: n }) : { value: string; }
>{ value: n } : { value: string; }
>value : string
>n : string

    later: { consumeAll: x => x.earlier.value.toLowerCase() },
>later : { consumeAll: (x: { earlier: { value: string; }; later: unknown; }) => string; }
>{ consumeAll: x => x.earlier.value.toLowerCase() } : { consumeAll: (x: { earlier: { value: string; }; later: unknown; }) => string; }
>consumeAll : (x: { earlier: { value: string; }; later: unknown; }) => string
>x => x.earlier.value.toLowerCase() : (x: { earlier: { value: string; }; later: unknown; }) => string
>x : { earlier: { value: string; }; later: unknown; }
>x.earlier.value.toLowerCase() : string
>x.earlier.value.toLowerCase : () => string
>x.earlier.value : string
>x.earlier : { value: string; }
>x : { earlier: { value: string; }; later: unknown; }
>earlier : { value: string; }
>value : string
>toLowerCase : () => string

});
r26.earlier.value.toLowerCase();
>r26.earlier.value.toLowerCase() : string
>r26.earlier.value.toLowerCase : () => string
>r26.earlier.value : string
>r26.earlier : { value: string; }
>r26 : { earlier: { value: string; }; later: unknown; }
>earlier : { value: string; }
>value : string
>toLowerCase : () => string

declare function pairs<T>(arg: {
>pairs : <T>(arg: { [K in keyof T]: [(n: string) => T[K], (x: T) => void]; }) => T
>arg : { [K in keyof T]: [(n: string) => T[K], (x: T) => void]; }

    [K in keyof T]: [(n: string) => T[K], (x: T) => void];
>n : string
>x : T

}): T;

const r27 = pairs({
>r27 : { earlier: { value: string; }; later: string; }
>pairs({    earlier: [n => ({ value: n }), x => {}],    later: [n => n, x => x.earlier.value.toLowerCase()],}) : { earlier: { value: string; }; later: string; }
>pairs : <T>(arg: { [K in keyof T]: [(n: string) => T[K], (x: T) => void]; }) => T
>{    earlier: [n => ({ value: n }), x => {}],    later: [n => n, x => x.earlier.value.toLowerCase()],} : { earlier: [(n: string) => { value: string; }, (x: { earlier: { value: string; }; later: string; }) => void]; later: [(n: string) => string, (x: { earlier: { value: string; }; later: string; }) => string]; }

    earlier: [n => ({ value: n }), x => {}],
>earlier : [(n: string) => { value: string; }, (x: { earlier: { value: string; }; later: string; }) => void]
>[n => ({ value: n }), x => {}] : [(n: string) => { value: string; }, (x: { earlier: { value: string; }; later: string; }) => void]
>n => ({ value: n }) : (n: string) => { value: string; }
>n : string
>({ value: n }) : { value: string; }
>{ value: n } : { value: string; }
>value : string
>n : string
>x => {} : (x: { earlier: { value: string; }; later: string; }) => void
>x : { earlier: { value: string; }; later: string; }

    later: [n => n, x => x.earlier.value.toLowerCase()],
>later : [(n: string) => string, (x: { earlier: { value: string; }; later: string; }) => string]
>[n => n, x => x.earlier.value.toLowerCase()] : [(n: string) => string, (x: { earlier: { value: string; }; later: string; }) => string]
>n => n : (n: string) => string
>n : string
>n : string
>x => x.earlier.value.toLowerCase() : (x: { earlier: { value: string; }; later: string; }) => string
>x : { earlier: { value: string; }; later: string; }
>x.earlier.value.toLowerCase() : string
>x.earlier.value.toLowerCase : () => string
>x.earlier.value : string
>x.earlier : { value: string; }
>x : { earlier: { value: string; }; later: string; }
>earlier : { value: string; }
>value : string
>toLowerCase : () => string

});
r27.earlier.value.toLowerCase();
>r27.earlier.value.toLowerCase() : string
>r27.earlier.value.toLowerCase : () => string
>r27.earlier.value : string
>r27.earlier : { value: string; }
>r27 : { earlier: { value: string; }; later: string; }
>earlier : { value: string; }
>value : string
>toLowerCase : () => string

r27.later.toLowerCase();
>r27.later.toLowerCase() : string
>r27.later.toLowerCase : () => string
>r27.later : string
>r27 : { earlier: { value: string; }; later: string; }
>later : string
>toLowerCase : () => string

