=== tests/cases/compiler/inferCheckTypeConstraint.ts ===
type AcceptStr<T extends string> = T
>AcceptStr : T

type Other<T extends string> = T extends infer R ? AcceptStr<R> : never
>Other : Other<T>

